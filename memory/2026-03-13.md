# 2026-03-13

## Config Recovery - OpenClaw JSON

- Reviewed `openclaw.json.damaged` vs current `openclaw.json`
- Current config is missing the `plugins` section that was in damaged file
- The damaged file had OpenClaw web-search plugin configured
- Current uses gemini for search instead
- **TODO:** Restore plugins section from damaged file

### From damaged file (to restore):
```json
"plugins": {
  "entries": {
    "openclaw-web-search": {
      "enabled": true,
      "config": {
        "apiKey": "4ac0cc39ea654de18091313bd1bec46d.yOH9ylC7mSfLCeJTAUp_zNbh"
      }
    }
  },
  "installs": {
    "openclaw-web-search": {
      "source": "npm",
      "spec": "@ollama/openclaw-web-search",
      "installPath": "/data/data/com.termux/files/home/.openclaw/extensions/openclaw-web-search",
      "version": "0.1.7"
    }
  }
}
```

Also missing from current:
- `skills.install.nodeManager: "npm"`
- `tools.profile: "full"` (currently "coding")

User said: "we will do it tomorrow"